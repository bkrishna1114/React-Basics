import React from 'react'

const About = ({name}) => {
  return (
 
      <div>
      <h2>This is the page that every one can login know the product details..</h2>
      <h3>{name}</h3>
      </div>
   
  )
}

export default About;